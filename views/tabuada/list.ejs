<%- include('../partials/header') %>

  <div class="starter-template">
    <h1>Escolha o seu número</h1>
    <ul>
      <% numeros.map((i => { %>
        <li><a href="/tabuadas/do/<%= i %>">Tabuda do <%= i %></a></li>
      <% })) %>
    </ul>
  </div>

<%- include('../partials/footer') %>
